
import LoadingComponent from './../../../components/loadingComponent/index';

export default function Loading() {
    return (
        <div>
            <LoadingComponent titulo="Carregando produto..."/>
        </div>
    );
}